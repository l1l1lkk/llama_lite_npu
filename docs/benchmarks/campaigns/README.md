# Canonical v2 Campaign 索引

本目录只收录 canonical v2 的中文 campaign 报告。每份报告必须链接 Git-tracked bundle、列出代码/环境/workload 指纹、严格门禁、异常和结论边界。

当前没有 `v0.0.15rc2` 正式性能报告。`benchmarks/configs/campaigns/p0_smoke.yaml` 是 dry-run 与 capability probe 的预备配置，不是性能结果。
