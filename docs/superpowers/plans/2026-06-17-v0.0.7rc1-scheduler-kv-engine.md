# v0.0.7rc1 Scheduler/KV Engine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add token-budget scheduling, KV metadata primitives, and a chunked-prefill planning entry for v0.0.7rc1.

**Architecture:** Keep model execution stable. Add pure control-plane primitives with unit tests first, then wire only safe optional parameters into continuous batching.

**Tech Stack:** Python, unittest, existing Lite Llama NPU continuous batching classes.

---

### Task 1: Token-budget scheduler

**Files:**
- Modify: `lite_llama/continuous_batching.py`
- Modify: `tests/test_continuous_batching.py`

- [ ] Add failing tests for `max_prefill_tokens` and `max_decode_tokens`.
- [ ] Add optional scheduler constructor arguments.
- [ ] Update admission and decode-step selection.
- [ ] Run continuous batching tests.

### Task 2: KV metadata engine

**Files:**
- Create: `lite_llama/kv_engine.py`
- Create: `tests/test_kv_engine.py`

- [ ] Add failing tests for refcounts, prefix matching, and chunk planning.
- [ ] Implement pure Python metadata classes.
- [ ] Run KV tests.

### Task 3: Documentation and versioning

**Files:**
- Modify: `VERSION`
- Modify: `CHANGELOG.md`
- Create: `docs/releases/v0.0.7rc1.md`
- Modify: `README.md`
- Modify: `README.zh.md`
- Modify: `docs/README.md`

- [ ] Record v0.0.7rc1 scope and limitations.
- [ ] Do not add unmeasured performance numbers.
- [ ] Run compile and unit verification.
