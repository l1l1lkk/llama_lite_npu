# v0.0.12rc3 发布记录

## 版本定位

v0.0.12rc3 是服务器实测阶段发现的问题修复版本，不改变 v0.0.11rc1 和 v0.0.12rc1 的优化目标。

## 问题描述

Top-P 并发 4 实测时，服务端在 TP worker 上崩溃：

```text
RuntimeError: Tensors must be contiguous
```

报错位置在 Decode 采样路径的 HCCL `all_gather`：

```text
lite_llama/sampling.py::_all_gather_stack
```

## 排查过程

Greedy 单并发可以正常完成，说明模型加载、NPU Graph、Continuous Batching 和基础通信链路正常。

Top-P 并发 4 在采样阶段触发候选 token 的 batched all-gather。该路径会从 `topk`、索引、切片等操作得到候选 logits 和 token ids，其中部分 tensor 在 NPU 上可能是 view 或非连续布局。

CPU 单测中 mock collectives 对 tensor layout 不敏感，因此没有暴露问题；HCCL 对 `all_gather` 输入更严格，要求输入 tensor 是 contiguous。

## 修复方式

在 `_all_gather_stack()` 的 collective 边界统一处理：

```python
if not tensor.is_contiguous():
    tensor = tensor.contiguous()
```

这样调用方不需要分别关心每一种候选 tensor 的来源，也避免后续新增采样路径再次踩同类问题。

## 验证

- 新增非连续 tensor 回归测试，模拟 HCCL 对 contiguous 的要求。
- 本地单测通过。
- 服务器容器内单测通过后，继续执行 Top-P 并发实测。

## 服务器实测记录

测试环境：

- 服务器：`218.106.157.54:10013`
- 容器：`triton-llama-lite`
- 代码目录：`/data/liuke/llama_lite_npu`
- 分支：`release/0.0.12rc1`
- Commit：`5af63d9`
- 模型：Qwen3-32B
- 硬件：2 张 Atlas 910B3
- TP：2
- NPU Graph：开启
- Continuous Batching：开启
- `sampling_candidate_k=2048`
- `chunked_prefill_policy=adaptive`
- `chunked_prefill_min_tokens=2048`

### Greedy 单并发

命令摘要：

```bash
evalscope perf \
  --url http://127.0.0.1:8213/v1/chat/completions \
  --api openai \
  --model Qwen3-32B \
  --dataset random \
  --number 20 \
  --parallel 1 \
  --min-prompt-length 184 \
  --max-prompt-length 184 \
  --max-tokens 256 \
  --temperature 0 \
  --stream
```

结果：

| 指标 | 数值 |
|---|---:|
| 成功率 | 20 / 20 |
| 平均输入 token | 206 |
| 平均输出 token | 256 |
| Output Throughput | 23.56 tok/s |
| Total Throughput | 42.52 tok/s |
| 平均延迟 | 10.86 s |
| TTFT | 987.2 ms |
| TPOT | 38.7 ms |
| ITL | 38.6 ms |

### Top-P 并发 4

命令摘要：

```bash
evalscope perf \
  --url http://127.0.0.1:8213/v1/chat/completions \
  --api openai \
  --model Qwen3-32B \
  --dataset random \
  --number 40 \
  --parallel 4 \
  --min-prompt-length 156 \
  --max-prompt-length 156 \
  --max-tokens 256 \
  --temperature 0.6 \
  --top-p 0.9 \
  --stream
```

结果：

| 指标 | 数值 |
|---|---:|
| 成功率 | 40 / 40 |
| 平均输入 token | 178 |
| 平均输出 token | 256 |
| Output Throughput | 60.51 tok/s |
| Total Throughput | 102.58 tok/s |
| 平均延迟 | 16.92 s |
| TTFT | 2103.7 ms |
| TPOT | 58.1 ms |
| ITL | 57.9 ms |

采样路径指标：

| 指标 | 数值 |
|---|---:|
| `lite_llama_sampling_candidate_rows_total` | 10350 |
| `lite_llama_sampling_fallback_rows_total` | 0 |
| `lite_llama_sampling_full_logit_gather_batches_total` | 0 |

结论：Top-P 并发 4 实测已经走 candidate sampling 路径，没有触发完整词表 gather fallback，说明 v0.0.11 的采样通信优化在该测试配置下生效。

## 与上一版本对比

| 项目 | v0.0.12rc2 | v0.0.12rc3 |
|---|---|---|
| 依赖版本 | 已对齐 | 不变 |
| Top-P candidate sampling | 可能传入非连续 tensor | collective 前强制连续 |
| Greedy 路径 | 正常 | 不变 |
| Adaptive Prefill | 正常 | 不变 |
